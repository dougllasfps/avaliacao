spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password: password
  h2:
    console:
      enabled: true
      path: /h2-console # URL de acesso ao console do H2
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
